import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    button: {
        position: 'absolute',
        padding: 10,
    },
    buttonText: {
        fontSize: 24,
        color: '#007AFF',
    },
});

export default styles;
