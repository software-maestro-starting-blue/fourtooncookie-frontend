import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    listContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        alignContent: 'flex-start',
        padding: 0,
        width: "100%",
        height: "auto",
    },
});
