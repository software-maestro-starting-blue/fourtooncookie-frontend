import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    content: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        borderWidth: 1,
        borderColor: '#000000',
        padding: 16,
        borderRadius: 8,
        marginBottom: 16,
      },
      diaryInput: {
        fontSize: 18,
        width: '100%',
        height: '100%',
      },
});