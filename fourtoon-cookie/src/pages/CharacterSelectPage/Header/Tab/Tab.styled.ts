import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    tab: {
        flex: 1,
        alignItems: 'center',
        paddingVertical: 10,
    },
    tabText: {
        fontSize: 16,
    },
    activeTabText: {
        fontWeight: 'bold',
        color: 'gray',
    },
    
});